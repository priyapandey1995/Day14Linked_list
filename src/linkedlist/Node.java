package linkedlist;

public class Node {
	/*  Node class will have the variables
	 * i.e data and Node(Node will point to the next node)
	 *  
	 * 
	 */
	
	int data;
	Node next;//so this Node is the reference for the next node
	
	Node(int data)
	{
		this.data = data;
	}
}
